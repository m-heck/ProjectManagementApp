/* TODO
import { SignInPageComponent } from './sign-in-page.component';

describe('PreLoginNavbarComponent', () => {
    it('Clicking in the sign in button should send an alert to the screen', () => {
        
        cy.mount(SignInPageComponent, {});
        //cy.get('#signup1').click();
        
        cy.on('window:alert', (t)=>{
            //assertions
            expect(t).to.contain('Yhas been signed up!');
         })
    });
})
*/